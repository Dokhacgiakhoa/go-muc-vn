# AGENT CONFIGURATION

**Quyền cao nhất (Highest Priority):**
- Tất cả các file `.md` (bao gồm `task.md`, `implementation_plan.md`, `walkthrough.md`, và các tài liệu khác) PHẢI được viết bằng TIẾNG VIỆT.
- Ngôn ngữ giao tiếp và nội dung kế hoạch cũng phải là Tiếng Việt.
- **TỰ ĐỘNG REVIEW UI/UX**: Sau mỗi lần chỉnh sửa code front-end, PHẢI tự động kiểm tra lại giao diện (sử dụng browser tool hoặc yêu cầu screenshot) để đảm bảo tính thẩm mỹ và trải nghiệm người dùng (UX) tốt nhất. Không được bỏ qua bước này.
